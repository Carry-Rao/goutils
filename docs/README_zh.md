We provide [English](https://github.com/Carry-Rao/goutils/blob/master/README.md)

# Goutils — 极简易用，无缝切换

本项目的核心设计初衷，是简化开发适配成本、统一开发规范，核心设计理念如下：

- 屏蔽底层复杂实现，降低开发接入门槛
- 支持多平台、多协议无感切换，适配多业务场景
- 提供标准化统一调用接口，统一编码风格
- 封装各类高频重复工具能力，减少冗余代码开发

## 数据库模块
该模块核心优势为**低侵入、无感切换**，开发者无需修改业务代码，即可灵活适配各类主流数据库，原生支持以下数据库：

- PostgreSQL
- MySQL
- SQLite

同时支持将缓存组件作为数据存储载体，拓展轻量化数据读写能力，适配缓存型业务场景：

- Redis 缓存
- Memory 内存缓存
- Bloom 布隆过滤器

除此之外，项目内置**Mixture 融合数据库**，支持多层级数据链路组合与异常策略定制，核心能力如下：

- 支持自定义多层数据链路（示例：过滤器 → 缓存 → 数据库），适配多级读写、过滤、降级场景
- 可配置多样化异常处理策略，包含自动降级、异常拦截过滤等能力，提升服务稳定性
- 全程保持统一调用接口，链路与策略变更不影响上层业务代码

[完整接口文档](https://github.com/Carry-Rao/goutils/blob/master/docs/database_zh.md)

## 日志模块

该模块提供了**统一的日志记录能力**，支持多级别日志、缓冲写入、颜色输出等特性。核心特性包括：

- **多级别日志**：支持 DEBUG、INFO、WARN、ERROR、PANIC 五个日志级别
- **缓冲写入**：支持缓冲区机制，减少频繁 IO 写入，提升性能
- **颜色输出**：支持终端颜色输出，便于日志分级查看
- **自动刷新**：PANIC 与 FATAL 级别日志自动刷新缓冲区并终止程序
- **级别过滤**：通过设置日志级别，灵活控制日志输出粒度

[完整接口文档](https://github.com/Carry-Rao/goutils/blob/master/docs/log_zh.md)
